import { InElec } from './inElec';
import { Time } from '@angular/common';

export class Upelection {
  firstName?: string;
  lastName?: string;
  // politicians?: InElec[];
  position?: string;
  dateTime?: Time;
  city?: string;
  state?: string;
}
