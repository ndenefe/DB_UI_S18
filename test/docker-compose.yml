version: '3'
services:
    mysql:
        build:
            context: ./sql       
        ports:
            - "3306:3306"
        volumes:
            - /sql
    node:
        depends_on:
            - "mysql"
        build:
            context: ./test
        ports:
            - "3000:3000"
